---
const path = Astro.url.pathname;
const isHome = path === '/';
const isBlog = path.startsWith('/blog');
const isAbout = path.startsWith('/about');
const isDoctorAI = path.startsWith('/doctor-ai');
---

<header class="topbar">
  <a href="/" class="tb-brand">S-Reborn World</a>

  <div class="tb-search-wrap" data-search-wrap>
    <svg class="tb-search-icon" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <circle cx="8.5" cy="8.5" r="5.5" stroke="currentColor" stroke-width="1.5"/>
      <path d="M13.5 13.5L17 17" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    </svg>
    <input
      id="search-input"
      class="tb-search"
      type="search"
      placeholder="글 검색..."
      autocomplete="off"
      aria-label="블로그 글 검색"
      aria-expanded="false"
      aria-controls="search-dropdown"
      aria-activedescendant=""
    />
    <kbd class="tb-search-shortcut">⌘K</kbd>
  </div>

  <nav class="tb-nav">
    <a href="/" class={`topbar-link${isHome ? ' active' : ''}`}>Home</a>
    <a href="/blog" class={`topbar-link${isBlog ? ' active' : ''}`}>Blog</a>
    <a href="/about" class={`topbar-link${isAbout ? ' active' : ''}`}>About</a>
    <a href="/doctor-ai/" class={`topbar-link doctor-ai-link${isDoctorAI ? ' active' : ''}`}>Doctor AI</a>
  </nav>
</header>

<div id="search-dropdown" class="search-dropdown hidden" role="listbox" aria-label="검색 결과">
  <div id="search-status" class="search-status hidden">검색 인덱스를 준비하는 중입니다...</div>
  <div id="search-results" class="search-results"></div>
  <div id="search-empty" class="search-empty hidden">
    <p class="search-empty-text">검색 결과가 없어요.</p>
    <button id="ai-generate-btn" class="ai-generate-btn" type="button">
      ✨ AI로 이 주제의 글 생성하기
    </button>
  </div>
</div>
